# Dependencies
node_modules

# Build output
dist
coverage
# Segundo reporte del gate de cobertura (cálculo puro de src/domain/**). Va a
# su propio directorio para no pisar el de src/lib/, y `coverage` no lo tapa:
# los patrones de .gitignore casan componentes completos, no prefijos.
coverage-domain
storybook-static

# Playwright (E2E) artifacts
playwright-report
test-results
.playwright

# Environment variables (contains secrets)
.env
.env.local
.env.*.local

# Editor
.vscode
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Supabase CLI cache
supabase/.temp/

# Claude Code background-agent worktrees (transient, harness-managed)
.claude/worktrees/

# infra:I14 condominios-media migration runtime artifact (rollback manifest)
condominios-media-manifest-*.json

# Estado de cookies del bypass E2E de Vercel (lo escribe e2e/bypass.setup.ts;
# contiene la cookie de sesión del Preview — jamás se versiona ni se sube)
e2e/.estado/
